import 'package:flutter/material.dart';

class UserTermsCondition extends StatelessWidget {
  const UserTermsCondition({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('User Terms & Conditions'),
    );
  }
}